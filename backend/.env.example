# Copy to backend/.env and fill in your key. The assistant panel in the
# editor returns a "no key" message until OPENAI_API_KEY is set.
OPENAI_API_KEY=

# Optional. Defaults to gpt-4o-mini.
OPENAI_MODEL=gpt-4o-mini
